{% extends "index.twig" %} {% block title %} Admin Hersteller - {{hersteller[0].name }} - Bearbeiten {% endblock %} {% block content %} {% include "extensions/AdminNav.twig" %}
<div class="row mb-4">
    <div class="col-md-5">
        <div class="card card-body">
            <div class="card-title">
                {{hersteller[0].name }} - Bearbeiten
            </div>
            <hr>
            <form action="/admin/hersteller/edit/{{ hersteller[0].id }}" method="post">
                <div class="row mb-3">
                    <div class="col">
                        <label for="name">Name:</label>
                        <input type="text" class="form-control" placeholder="{{ hersteller[0].name }}" name="name" id="name" value="{{ hersteller[0].name }}">
                    </div>
                </div>
                <button type="submit" class="btn btn-success">Speichern</button>
            </form>
        </div>
    </div>
</div>
{% endblock %}